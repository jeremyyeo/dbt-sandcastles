name: "databricks"
version: "1.0.0"
config-version: 2

profile: "databricks"

macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"

models:
  databricks:
    +file_format: delta
    +materialized: table
